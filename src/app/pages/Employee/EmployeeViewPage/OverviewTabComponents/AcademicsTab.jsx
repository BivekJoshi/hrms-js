import React from 'react';
// import { useGetQualificationById } from '../../../../hooks/employee/useEmployee';
import { useParams } from 'react-router-dom';

const AcademicsTab = () => {
	<div>Academics</div>;
	// const { id } = useParams();
	// const { data: qualificationData, isLoading } = useGetQualificationById(id);
	// console.log(qualificationData);
	// return <>{!isLoading && qualificationData[0]?.board}</>;
};

export default AcademicsTab;
